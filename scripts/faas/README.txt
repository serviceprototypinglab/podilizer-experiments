How to run:

1. Copy appropriate script into translated-built-updated project folder
2. Run the script

[Important!] For maths project before running, move 'awsl' and 'unpackaged' directories into 'src' folder. Maths project also needs testng.jar and jcommander.jar in '/usr/share/java/'.
